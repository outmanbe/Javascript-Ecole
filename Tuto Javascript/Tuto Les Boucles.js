// LES BOUCLES //

// SI = if
// Sinon = if else
// Tant que = while

let i = 0;              // initialiser la variable

while(i < 5)           // Tant que i est plus petit que 5
{                      // instructions

}                      // fin instructions

//--------------------------------------------------//


let z = 0;              // initialiser la variable et sa valeur

while(z < 5)           // Tant que i est plus petit que 5
{                      // instructions
    document.write("a ");
    i++;            // Incrémentation de 1 
}                   // fin instructions

//------------------------------------------------------------------//

let a = 10;

do                           // Faire tant que ( spécificité a la boucle while)
{
    document.write("AAA ");
    i++;
}
while(a < 10);

//-------------------------------------------------------------------//

for(i = 0 ; i < 5 ; ++i )                                     // Pour : (initialisation (compteur dans l'exemple ; expression a verifier ; l'opération a effectuer en fin d'iteration )
{
    document.write("a ");
}

//------------------------------------------------------------//
